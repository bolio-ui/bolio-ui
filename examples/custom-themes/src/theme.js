import { Themes } from '@bolio-ui/core'

export const blueTheme = Themes.createFromLight({
  type: 'blue',
  palette: {
    success: 'blue',
    warning: 'blue',
    error: 'blue'
  }
})

export const violetTheme = Themes.createFromLight({
  type: 'violet',
  palette: {
    success: 'violet',
    warning: 'violet',
    error: 'violet'
  }
})
