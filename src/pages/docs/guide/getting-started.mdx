import Docs from 'src/templates/Docs'
import { Attributes, Anchor } from 'src/components'
import { Tabs, Spacer, Snippet } from 'core'

export const meta = {
  title: 'Getting Started',
  description:
    'Welcome to the Bolio UI documentation! Bolio UI allows you to make beautiful, modern, and fast websites/applications regardless of your design experience.',
  sidebar: 'guide',
  group: 'Guide'
}

## Getting started

<Spacer h={2} />

#### Welcome to the Bolio UI documentation!

Bolio UI allows you to make beautiful, modern, and fast websites/applications regardless of your design experience, created with <a href="https://reactjs.org/" rel="noreferrer" target="_blank">React.js</a>.

<Spacer h={2} />

### Install

Inside your React project directory, install Bolio UI by running either of the following:

<Spacer h={3} />

<Tabs initialValue="yarn" hideDivider hideBorder align="flex-start" mt="-46px">
  <Tabs.Item label="Yarn" value="yarn">
    <Snippet text="yarn add @bolio-ui/core" />
  </Tabs.Item>
  <Tabs.Item label="Npm" value="npm">
    <Snippet text="npm i @bolio-ui/core" />
  </Tabs.Item>
</Tabs>

<Spacer h={3} />

### Setup

For Bolio UI to work correctly, you need to set up the `BolioUIProvider` and the `CssBaseline` at the root of your application.

<Spacer h={1} />

#### React

Go to the root of your application and do this:

```tsx
// NAME: /src/app.js
import * as React from 'react'

// 1. import `BolioUIProvider` and `CssBaseline`
import { BolioUIProvider, CssBaseline } from '@bolio-ui/core'

function App({ Component }) {
  // 2. Use at the root of your app
  return (
    <BolioUIProvider>
      <CssBaseline />
      <Component />
    </BolioUIProvider>
  )
}
```

<Spacer h={2} />

#### Next.js

##### Pages Router

Go to `pages/_app.js` or `pages/_app.tsx` (create it if it doesn't exist) and add this:

```tsx
// 1. import `BolioUIProvider` and `CssBaseline`
import { BolioUIProvider, CssBaseline } from '@bolio-ui/core'

function MyApp({ Component, pageProps }) {
  return (
    // 2. Use at the root of your app
    <BolioUIProvider>
      <CssBaseline />
      <Component {...pageProps} />
    </BolioUIProvider>
  )
}

export default MyApp
```

You don't need a custom `_document`, Next.js already adds the styles to the HTML.

##### App Router

Go to `app/layout.js` or `app/layout.tsx` and add this:

```tsx
// 1. import `BolioUIProvider`, `CssBaseline` and the styles registry
import { BolioUIProvider, CssBaseline } from '@bolio-ui/core'
import { StyledJsxRegistry } from '@bolio-ui/core/next'

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {/* 2. The registry adds the styles rendered on the server to the HTML */}
        <StyledJsxRegistry>
          <BolioUIProvider>
            <CssBaseline />
            {children}
          </BolioUIProvider>
        </StyledJsxRegistry>
      </body>
    </html>
  )
}
```

> For more information about Next.js + Bolio UI SSR check out the [documentation](/guide/bolio-ui-plus-nextjs).

<Spacer h={2} />

### Using Bolio UI components

Once Bolio UI is installed you can use any of the components as follows.
Bolio UI uses tree-shaking so the unused modules will not be included in the bundle during the build process and
each component is exported separately.

```tsx
import { Button } from '@bolio-ui/core'

const Component = () => <Button>Click me</Button>
```

<Spacer h={2} />

<Attributes edit="/pages/docs/components/avatar.mdx">
<Attributes.Title>BolioUIProvider.Props</Attributes.Title>

| Attribute     | Type                   | Accepted values                                 | Description                                                  | Default |
| ------------- | ---------------------- | ----------------------------------------------- | ------------------------------------------------------------ | ------- |
| **themes**    | `Array<BolioUIThemes>` | -                                               | Custom themes added to the default `light` and `dark` themes | -       |
| **themeType** | `string`               | `light`, `dark` or the `type` of a custom theme | Theme applied to every component                             | `light` |

</Attributes>

<Spacer h={3} />

### Community

We're excited to see the community adopt Bolio UI, raise issues, and provide feedback.
Whether it's a feature request, bug report, or a project to showcase, please get involved!

- [Instagram](https://instagram.com/bolio.ui)
- [GitHub](https://github.com/bolio-ui/bolio-ui)

<Spacer h={3} />

### Contributing

PR's on Bolio UI are always welcome, please see our [contribution guidelines](https://github.com/bolio-ui/bolio-ui/blob/main/CONTRIBUTING.MD) to learn how you can contribute to this project.

export default Docs.withMeta(meta)
