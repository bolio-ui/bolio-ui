import Docs from 'src/templates/Docs'
import { Link } from 'core'

export const meta = {
  title: 'Bolio UI + RedwoodJS',
  description: '',
  sidebar: 'guide',
  group: 'Guide',
  index: 2
}

## Bolio UI + RedwoodJS

Bolio UI is totally compatible with <Link target="_blank" color rel="nofollow" href="https://nextjs.org">Next.js</Link>.

export default ({ children }) => <Docs meta={meta}>{children}</Docs>
