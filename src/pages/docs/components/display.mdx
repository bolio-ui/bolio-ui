import Docs from 'src/templates/Docs'
import { Playground, Attributes } from 'src/components'
import { Display, Code, Snippet, Image, Grid } from 'core'

export const meta = {
  title: 'Display',
  description:
    'To display important information or images in a central alignment.',
  sidebar: 'components',
  group: 'Components',
  index: 13
}

## Display

To display important information or images in a central alignment.

<Playground
  title="General"
  desc="Describe the main part with a paragraph."
  scope={{ Display, Image, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid md={12}>
    <Display shadow caption="Make your development more creative and dynamic with amazing tools for React. 🥷🏼">
      <Image height="200px" src="https://images.pexels.com/photos/1779487/pexels-photo-1779487.jpeg" />
    </Display>
  </Grid>
</Grid.Container>  
`}
/>

<Playground
  title="With Code"
  desc="Show multiline code blocks."
  scope={{ Display, Code, Grid }}
  code={`
() => {
  const codes = \`{
  "build": {
    "env": {
      "VARIABLE": "ENV_SECRET"
    }
  }
}\`
  return (
    <Grid.Container gap={2}>
      <Grid md={12}>
        <Display width="500px" caption="Application's build with a defined secret environment variable.">
          <Code block>{codes}</Code>
        </Display>
      </Grid>
    </Grid.Container>
  )
}
`}
/>

<Playground
  title="With Snippet"
  desc="Show shell codes."
  scope={{ Display, Snippet, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid md={12}>
    <Display width="500px" caption="Run this command to install the library.">
      <Snippet type="dark">yarn add @bolio-ui/core</Snippet>
    </Display>
  </Grid>
</Grid.Container> 
`}
/>

<Playground
  title="Component Caption"
  desc="Use component on `caption` prop."
  scope={{ Display, Code, Image, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid md={12}>
    <Display shadow caption={<p>Browse <Code>bolio-ui.com.br</Code> for more information.</p>}>
      <Image width="650px" height="297px" src="https://images.pexels.com/photos/1779487/pexels-photo-1779487.jpeg" />
    </Display>
  </Grid>
</Grid.Container>
`}
/>

<Attributes edit="/pages/docs/components/display.mdx">
<Attributes.Title>Display.Props</Attributes.Title>

| Attribute   | Description                   | Type                   | Accepted values | Default |
| ----------- | ----------------------------- | ---------------------- | --------------- | ------- |
| **caption** | description for display block | `ReactNode` / `string` | -               | -       |
| **shadow**  | show shadow                   | `boolean`              | -               | `false` |
| ...         | native props                  | `HTMLAttributes`       | -               | -       |

</Attributes>

export default ({ children }) => <Docs meta={meta}>{children}</Docs>
