import Docs from 'src/templates/Docs'
import { Playground, Attributes } from 'src/components'
import { Link, Text, Code, Grid } from 'core'
import NextLink from 'next/link'

export const meta = {
  title: 'Link',
  description: 'Hyperlinks between pages.',
  sidebar: 'components',
  group: 'Components',
  index: 22
}

## Link

Hyperlinks between pages.

<Playground
  scope={{ Link, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Link href="#">
      Is a reference to the data you can follow directly by tapping.
    </Link>
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Highlight"
  desc="Distinguish links with different colors."
  scope={{ Link, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Link href="#" color>
      Is a reference to the data you can follow directly by tapping.
    </Link>
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="With Icon"
  desc="Show an icon in the link."
  scope={{ Link, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Link href="#" icon>
      Is a reference to the data you can follow directly by tapping.
    </Link>
  </Grid>
  <Grid>
    <Link href="#" icon color>
      Is a reference to the data you can follow directly by tapping.
    </Link>
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Variant"
  desc="Different styles of links."
  scope={{ Link, Text, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Text>
      <Link href="#">
        Is a reference to the data you can follow directly by tapping.
      </Link>
    </Text>
  </Grid>
  <Grid>
    <Text>
      <Link href="#" color>
        Is a reference to the data you can follow directly by tapping.
      </Link>
    </Text>
  </Grid>
  <Grid>
    <Text>
      <Link href="#" underline>
        Is a reference to the data you can follow directly by tapping.
      </Link>
    </Text>
  </Grid>
  <Grid>
    <Text>
      <Link href="#" color underline>
        Is a reference to the data you can follow directly by tapping.
      </Link>
    </Text>
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Block"
  desc="Display as a separate block."
  scope={{ Link, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Link href="#" block>
      Is a reference to the data you can follow directly by tapping.
    </Link>
  </Grid>
</Grid.Container>
`}
/>

<Playground
  /* title={
    <>
      With <Code>next/link</Code>
    </>
  } */
  title="With Next/Link"
  desc="Use `Link` with NextJS without any configuration."
  scope={{ Link, NextLink, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <NextLink href="/docs/components/button">
      <Link block>Docs of Button</Link>
    </NextLink>
  </Grid>
</Grid.Container>
`}
/>

<Attributes edit="/pages/docs/components/link.mdx">
<Attributes.Title>Link.Props</Attributes.Title>

| Attribute     | Description                 | Type                                            | Accepted values        | Default |
| ------------- | --------------------------- | ----------------------------------------------- | ---------------------- | ------- |
| **href**      | link url                    | `string`                                        | -                      | -       |
| **color**     | display color for link      | `boolean`                                       | -                      | `false` |
| **icon**      | show icon                   | `boolean`                                       | -                      | `false` |
| **underline** | display underline           | `boolean`                                       | -                      | `false` |
| **block**     | display as a separate block | `boolean`                                       | -                      | `false` |
| **ref**       | forwardRef                  | <Code>Ref<HTMLAnchorElement &#124; null></Code> | -                      | -       |
| ...           | native props                | `AnchorHTMLAttributes`                          | `'rel', 'target', ...` | -       |

</Attributes>

export default ({ children }) => <Docs meta={meta}>{children}</Docs>
