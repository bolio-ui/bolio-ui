import Docs from 'src/templates/Docs'
import { Playground, Attributes } from 'src/components'
import { Toggle, Grid } from 'core'

export const meta = {
  title: 'Toggle',
  description: 'Displays a boolean value.',
  sidebar: 'components',
  group: 'Components',
  index: 40
}

## Toggle

Displays a boolean value.

<Playground
  desc="The basic `Toggle` usage."
  scope={{ Toggle, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Toggle scale={2} />
  </Grid>
  <Grid>
    <Toggle scale={2} initialChecked />
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Type"
  desc="Differentiate states by color."
  scope={{ Toggle, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Toggle type="default" scale={2} initialChecked />
  </Grid>
  <Grid>
    <Toggle type="primary" scale={2} initialChecked />
  </Grid>
  <Grid>
    <Toggle type="secondary" scale={2} initialChecked />
  </Grid>
  <Grid>
    <Toggle type="success" scale={2} initialChecked />
  </Grid>
  <Grid>
    <Toggle type="warning" scale={2} initialChecked />
  </Grid>
  <Grid>
    <Toggle type="error" scale={2} initialChecked />
  </Grid>
  <Grid>
    <Toggle type="info" scale={2} initialChecked />
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Disabled"
  desc="You can change disabled `Toggle` with the property `disabled`."
  scope={{ Toggle, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Toggle scale={2} disabled />
  </Grid>
  <Grid>
    <Toggle scale={2} initialChecked disabled />
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Get Change"
  desc="Capture changes in `Toggle`."
  scope={{ Toggle, Grid }}
  code={`
() => {
  const handler = event => {
    console.log(event.target.checked)
  }
  return (
    <Grid.Container gap={2}>
      <Grid>
        <Toggle onChange={handler} scale={2} />
      </Grid>
    </Grid.Container>
  )
}
`}
/>

<Attributes edit="/pages/docs/components/toggle.mdx">
<Attributes.Title>Toggle.Props</Attributes.Title>

| Attribute          | Description               | Type                  | Accepted values                    | Default   |
| ------------------ | ------------------------- | --------------------- | ---------------------------------- | --------- |
| **checked**        | checked or not            | `boolean`             | -                                  | -         |
| **initialChecked** | checked or not on initial | `boolean`             | -                                  | `false`   |
| **onChange**       | change event handler      | `ToggleEvent`         | -                                  | -         |
| **type**           | current type              | `ToggleTypes`         | [ToggleTypes](#toggletypes)        | `default` |
| **disabled**       | disable toggle            | `boolean`             | -                                  | `false`   |
| ...                | native props              | `LabelHTMLAttributes` | `'from', 'name', 'className', ...` | -         |

<Attributes.Title>ToggleEvent</Attributes.Title>

```ts
interface ToggleEventTarget {
  checked: boolean
}

export interface ToggleEvent {
  target: ToggleEventTarget
  stopPropagation: () => void
  preventDefault: () => void
  nativeEvent: React.ChangeEvent
}
```

<Attributes.Title>ToggleTypes</Attributes.Title>

```ts
type ToggleTypes =
  | 'default'
  | 'primary'
  | 'secondary'
  | 'success'
  | 'warning'
  | 'error'
  | 'info'
```

</Attributes>

export default ({ children }) => <Docs meta={meta}>{children}</Docs>
