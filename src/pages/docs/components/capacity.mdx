import Docs from 'src/templates/Docs'
import { Playground, Attributes } from 'src/components'
import { Capacity, Grid, useTheme } from 'core'

export const meta = {
  title: 'Capacity',
  description: 'Display an capacity indicator.',
  sidebar: 'components',
  group: 'Components',
  index: 7
}

## Capacity

Display an capacity indicator.

<Playground
  title="General"
  desc="The basic `Capacity` usage."
  scope={{ Capacity, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Capacity value={15} width="200px" />
  </Grid>
  <Grid>
    <Capacity value={45} width="200px" />
  </Grid>
  <Grid>
    <Capacity value={95} width="200px" />
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Fixed Color"
  desc="Specify a color to override the default."
  scope={{ Capacity, Grid, useTheme }}
  code={`
() => {
  const theme = useTheme()
  return (
    <Grid.Container gap={2}>
      <Grid>
        <Capacity value={75} color={theme.palette.success} width="200px" />
      </Grid>
    </Grid.Container>
  )
}
`}
/>

<Attributes edit="/pages/docs/components/capacity.mdx">
<Attributes.Title>Capacity.Props</Attributes.Title>

| Attribute | Description      | Type             | Accepted values    | Default |
| --------- | ---------------- | ---------------- | ------------------ | ------- |
| **value** | value number     | `number`         | -                  | 0       |
| **limit** | max value        | `number`         | -                  | 100     |
| **color** | css color string | `string`         | -                  | -       |
| ...       | native props     | `HTMLAttributes` | `'className', ...` | -       |

</Attributes>

export default ({ children }) => <Docs meta={meta}>{children}</Docs>
