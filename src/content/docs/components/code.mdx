import Docs from 'src/templates/Docs'
import { Code, Text, Grid } from 'core'
import { Playground, Attributes, ParsedCodes } from 'src/components'
import WaitTime from 'core/MdxWidgets/ParsedCodes/WaitTime'

export const meta = {
  title: 'Code',
  description: 'Show source code in a standardized way.',
  sidebar: 'components',
  group: 'Components',
  index: 10
}

## Code

Show source code in a standardized way. Want to display shell code snippets? try [Snippet](/docs/components/snippet).

<Playground
  desc="Basic inline codes."
  scope={{ Code, Text, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Text my={0}>
      Run <Code>npm i @bolio-ui/core</Code> to install.
    </Text>
    <Text my={0}>
      Or run <Code>yarn add @bolio-ui/core</Code> to install.
    </Text>
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Code Block"
  desc="Multi line code display."
  scope={{ Code, WaitTime, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Code block my={0}>
      <WaitTime />
    </Code>
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Width"
  desc="Specify width manually, or other styles."
  scope={{ Code, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid xs={24}>
    <Code block width="50%" my={0}>
      {'yarn add @bolio-ui/core'}
    </Code>
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Name"
  desc="Specify the file name for the code block."
  scope={{ Code, WaitTime, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Code block name="/Components/WaitTime.jsx" my={0}>
      <WaitTime />
    </Code>
  </Grid>
</Grid.Container>
`}
/>

<Playground
  title="Tabs"
  desc="Show more than one code in the same block. Set the names in `tabs` and change the code when `onTabChange` is called."
  scope={{ Code, Grid }}
  code={`
() => {
  const [active, setActive] = React.useState(0)
  const codes = [
    "import { Button } from '@bolio-ui/core'",
    "import { Text } from '@bolio-ui/core'"
  ]
  return (
    <Grid.Container gap={2}>
      <Grid>
        <Code
          block
          tabs={['/src/button.js', '/src/text.js']}
          activeTab={active}
          onTabChange={setActive}
          my={0}
        >
          {codes[active]}
        </Code>
      </Grid>
    </Grid.Container>
  )
}
`}
/>

<Playground
  title="Classic Style"
  desc="The classic design style with a line border instead of a background."
  scope={{ Code, WaitTime, Grid }}
  code={`
<Grid.Container gap={2}>
  <Grid>
    <Code block classic name="/Components/WaitTime.jsx" my={0}>
      <WaitTime />
    </Code>
  </Grid>
</Grid.Container>
`}
/>

<Attributes edit="/content/docs/components/code.mdx">
<Attributes.Title>Code.Props</Attributes.Title>

| Attribute       | Description                                 | Type                      | Accepted values    | Default |
| --------------- | ------------------------------------------- | ------------------------- | ------------------ | ------- |
| **block**       | show code in `pre`                          | `boolean`                 | -                  | `false` |
| **name**        | text displayed at the top of the code block | `string`                  | -                  | -       |
| **classic**     | display with classic style                  | `boolean`                 | -                  | `false` |
| **tabs**        | names of the tabs, in place of `name`       | `string[]`                | -                  | `[]`    |
| **activeTab**   | index of the active tab                     | `number`                  | -                  | `0`     |
| **onTabChange** | called with the index of the chosen tab     | `(index: number) => void` | -                  | -       |
| ...             | native props                                | `HTMLAttributes`          | `'className', ...` | -       |

</Attributes>

export default Docs.withMeta(meta)
